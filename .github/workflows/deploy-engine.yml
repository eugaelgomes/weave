name: Deploy Engine

on:
  push:
    branches:
      - main
    paths:
      - "weave-engine/**"
      - "compose.engine.yml"
      - "docs/vault-deploy.md"
      - ".github/workflows/deploy-engine.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-engine-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-engine:
    name: Validate engine
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: weave-engine

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: weave-engine/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Check
        run: npm run check

  deploy-engine:
    name: Deploy engine
    runs-on: ubuntu-latest
    needs: validate-engine
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Validate deploy secrets
        env:
          ENGINE_REMOTE_HOST: ${{ secrets.ENGINE_REMOTE_HOST }}
          ENGINE_REMOTE_USER: ${{ secrets.ENGINE_REMOTE_USER }}
          ENGINE_SSH_PRIVATE_KEY: ${{ secrets.ENGINE_SSH_PRIVATE_KEY }}
          ENGINE_APP_DIR: ${{ secrets.ENGINE_APP_DIR }}
          VAULT_ADDR: ${{ secrets.VAULT_ADDR }}
          VAULT_ROLE_ID: ${{ secrets.VAULT_ROLE_ID }}
          VAULT_SECRET_ID: ${{ secrets.VAULT_SECRET_ID }}
          ENGINE_VAULT_SECRET_PATH: ${{ secrets.ENGINE_VAULT_SECRET_PATH }}
        run: |
          set -e
          [ -n "$ENGINE_REMOTE_HOST" ] || { echo "Missing secret: ENGINE_REMOTE_HOST" >&2; exit 1; }
          [ -n "$ENGINE_REMOTE_USER" ] || { echo "Missing secret: ENGINE_REMOTE_USER" >&2; exit 1; }
          [ -n "$ENGINE_SSH_PRIVATE_KEY" ] || { echo "Missing secret: ENGINE_SSH_PRIVATE_KEY" >&2; exit 1; }
          [ -n "$ENGINE_APP_DIR" ] || { echo "Missing secret: ENGINE_APP_DIR" >&2; exit 1; }
          if [ -n "$VAULT_ADDR" ] || [ -n "$VAULT_ROLE_ID" ] || [ -n "$VAULT_SECRET_ID" ] || [ -n "$ENGINE_VAULT_SECRET_PATH" ]; then
            [ -n "$VAULT_ADDR" ] || { echo "Missing secret: VAULT_ADDR" >&2; exit 1; }
            [ -n "$VAULT_ROLE_ID" ] || { echo "Missing secret: VAULT_ROLE_ID" >&2; exit 1; }
            [ -n "$VAULT_SECRET_ID" ] || { echo "Missing secret: VAULT_SECRET_ID" >&2; exit 1; }
            [ -n "$ENGINE_VAULT_SECRET_PATH" ] || { echo "Missing secret: ENGINE_VAULT_SECRET_PATH" >&2; exit 1; }
          fi

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.ENGINE_REMOTE_HOST }}
          username: ${{ secrets.ENGINE_REMOTE_USER }}
          key: ${{ secrets.ENGINE_SSH_PRIVATE_KEY }}
          script: |
            set -e

            APP_DIR="${{ secrets.ENGINE_APP_DIR }}"
            VAULT_ADDR="${{ secrets.VAULT_ADDR }}"
            VAULT_ROLE_ID="${{ secrets.VAULT_ROLE_ID }}"
            VAULT_SECRET_ID="${{ secrets.VAULT_SECRET_ID }}"
            VAULT_SECRET_PATH="${{ secrets.ENGINE_VAULT_SECRET_PATH }}"
            if [ -z "$APP_DIR" ]; then
              echo "ENGINE_APP_DIR is not set" >&2
              exit 1
            fi

            REPO_URL="https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git"

            mkdir -p "$APP_DIR"
            cd "$APP_DIR"
            if [ -d ".git" ]; then
              git remote set-url origin "$REPO_URL"
            else
              git init
              git remote add origin "$REPO_URL"
            fi
            git fetch origin
            git checkout -B main origin/main
            git reset --hard origin/main

            # Non-interactive SSH often skips login PATH (e.g. Docker in /usr/local or snap).
            [ -f /etc/profile ] && . /etc/profile
            [ -f "$HOME/.profile" ] && . "$HOME/.profile"
            export PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/snap/bin:$PATH"

            if [ -n "$VAULT_ADDR" ] && [ -n "$VAULT_ROLE_ID" ] && [ -n "$VAULT_SECRET_ID" ] && [ -n "$VAULT_SECRET_PATH" ]; then
              echo "Generating weave-engine/.env from Vault..."
              LOGIN_RESPONSE="$(curl -fsS --request POST --header "Content-Type: application/json" --data "{\"role_id\":\"$VAULT_ROLE_ID\",\"secret_id\":\"$VAULT_SECRET_ID\"}" "$VAULT_ADDR/v1/auth/approle/login")"
              VAULT_TOKEN="$(python3 -c 'import json,sys; print(json.load(sys.stdin)["auth"]["client_token"])' <<<"$LOGIN_RESPONSE")"
              SECRET_RESPONSE="$(curl -fsS --header "X-Vault-Token: $VAULT_TOKEN" "$VAULT_ADDR/v1/$VAULT_SECRET_PATH")"
              mkdir -p weave-engine
              python3 -c 'import json,sys; data=json.load(sys.stdin)["data"]["data"]; [print(f"{k}={v}") for k,v in data.items()]' <<<"$SECRET_RESPONSE" > weave-engine/.env
              chmod 600 weave-engine/.env
            else
              [ -f weave-engine/.env ] || { echo "weave-engine/.env not found and Vault is not configured" >&2; exit 1; }
            fi

            if command -v docker >/dev/null 2>&1 && docker compose version >/dev/null 2>&1; then
              docker compose -f compose.engine.yml up -d --build
            elif command -v docker-compose >/dev/null 2>&1; then
              docker-compose -f compose.engine.yml up -d --build
            else
              echo "Docker not found or Compose plugin missing. On the server, install Docker Engine" >&2
              echo "and ensure 'docker compose' works for this SSH user (same as in an interactive shell)." >&2
              exit 127
            fi
