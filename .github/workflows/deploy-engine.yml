name: Deploy Engine

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
    paths:
      - "weave-engine/**"
      - "compose.engine.yml"
      - ".github/workflows/deploy-engine.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: deploy-engine-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-engine:
    name: Validate engine
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: weave-engine

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: weave-engine/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Check
        run: npm run check

  build-images:
    name: Build & Push Engine Image
    runs-on: ubuntu-latest
    needs: [validate-engine]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/weave-engine
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push engine image
        uses: docker/build-push-action@v5
        with:
          context: ./weave-engine
          file: ./weave-engine/Dockerfile
          push: true
          platforms: linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy engine
    runs-on: ubuntu-latest
    needs: build-images
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Validate deploy secrets
        env:
          ENGINE_REMOTE_HOST: ${{ secrets.ENGINE_REMOTE_HOST }}
          ENGINE_REMOTE_USER: ${{ secrets.ENGINE_REMOTE_USER }}
          ENGINE_SSH_PRIVATE_KEY: ${{ secrets.ENGINE_SSH_PRIVATE_KEY }}
          ENGINE_APP_DIR: ${{ secrets.ENGINE_APP_DIR }}
          ENGINE_DOPPLER_TOKEN: ${{ secrets.ENGINE_DOPPLER_TOKEN }}
        run: |
          set -e
          [ -n "$ENGINE_REMOTE_HOST" ] || { echo "Missing secret: ENGINE_REMOTE_HOST" >&2; exit 1; }
          [ -n "$ENGINE_REMOTE_USER" ] || { echo "Missing secret: ENGINE_REMOTE_USER" >&2; exit 1; }
          [ -n "$ENGINE_SSH_PRIVATE_KEY" ] || { echo "Missing secret: ENGINE_SSH_PRIVATE_KEY" >&2; exit 1; }
          [ -n "$ENGINE_APP_DIR" ] || { echo "Missing secret: ENGINE_APP_DIR" >&2; exit 1; }
          [ -n "$ENGINE_DOPPLER_TOKEN" ] || { echo "Missing secret: ENGINE_DOPPLER_TOKEN" >&2; exit 1; }

      - name: Create App Directory
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.ENGINE_REMOTE_HOST }}
          username: ${{ secrets.ENGINE_REMOTE_USER }}
          key: ${{ secrets.ENGINE_SSH_PRIVATE_KEY }}
          script: |
            mkdir -p "${{ secrets.ENGINE_APP_DIR }}"

      - name: Copy Compose file via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.ENGINE_REMOTE_HOST }}
          username: ${{ secrets.ENGINE_REMOTE_USER }}
          key: ${{ secrets.ENGINE_SSH_PRIVATE_KEY }}
          source: "compose.engine.yml"
          target: ${{ secrets.ENGINE_APP_DIR }}

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.ENGINE_REMOTE_HOST }}
          username: ${{ secrets.ENGINE_REMOTE_USER }}
          key: ${{ secrets.ENGINE_SSH_PRIVATE_KEY }}
          script: |
            set -e

            APP_DIR="${{ secrets.ENGINE_APP_DIR }}"
            cd "$APP_DIR"

            # Create or update .env file
            echo "DOPPLER_TOKEN=${{ secrets.ENGINE_DOPPLER_TOKEN }}" > .env

            # Non-interactive SSH often skips login PATH (e.g. Docker in /usr/local or snap).
            [ -f /etc/profile ] && . /etc/profile
            [ -f "$HOME/.profile" ] && . "$HOME/.profile"
            export PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/snap/bin:$PATH"


            # Authenticate with GHCR
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            if command -v docker >/dev/null 2>&1 && docker compose version >/dev/null 2>&1; then
              COMPOSE="docker compose"
            elif command -v docker-compose >/dev/null 2>&1; then
              COMPOSE="docker-compose"
            else
              echo "Docker not found or Compose plugin missing. On the server, install Docker Engine" >&2
              echo "and ensure 'docker compose' works for this SSH user (same as in an interactive shell)." >&2
              exit 127
            fi

            echo "▶ Pulling engine image..."
            $COMPOSE -f compose.engine.yml pull

            echo "▶ Deploying engine..."
            $COMPOSE -f compose.engine.yml up -d

            echo "▶ Reloading Caddy proxy (if running)..."
            $COMPOSE -f compose.server.yml exec -T caddy caddy reload --config /etc/caddy/Caddyfile || true

            # Keep cleanup bounded so deploy does not hit SSH action timeout.
            if command -v timeout > /dev/null 2>&1; then
              timeout 60s docker image prune -f || true
              timeout 60s docker builder prune -f || true
            else
              echo "Skipping Docker prune: 'timeout' command not available."
            fi

            echo "✔ Engine deployed successfully."
