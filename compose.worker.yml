services:
  worker:
    image: ghcr.io/eugaelgomes/weave-worker:latest
    restart: always
    env_file:
      - path: ./.env
        required: false
      - path: ./weave-worker/.env
        required: false
    environment:
      - NODE_ENV=production
      - DOPPLER_CONFIG=prd
