/**
 * @module weave-engine/modules/core/orchestration/multi-agent
 * @description Exports the generic StateGraph multi-agent orchestration engine.
 */
const { StateGraph, END } = require("./state-graph");

module.exports = {
  StateGraph,
  END,
};
