const {
  env,
  hasDatabaseConfig,
  optionalEnvVars,
  requiredEnvVars,
  validateEnv,
} = require("./enviroments");

module.exports = {
  env,
  hasDatabaseConfig,
  optionalEnvVars,
  requiredEnvVars,
  validateEnv,
};
